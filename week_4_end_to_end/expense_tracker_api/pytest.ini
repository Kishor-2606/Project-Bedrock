# pytest.ini
# ---------
# Tells pytest where to find tests and which directories contain app code.
# Run tests from the expense_tracker_api/ directory:
#   pytest tests/ -v

[pytest]
testpaths = tests
pythonpath = .
